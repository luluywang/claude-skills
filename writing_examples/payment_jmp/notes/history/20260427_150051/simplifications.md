# Simplifications

## appendix_credit_debit.tex

Top priority: replace every "post-Durbin null" / "the null" / "post-Durbin data" nominalization with a plain verb phrase describing what actually happened in the data ("credit interchange did not fall when Durbin halved debit interchange," or similar in-context paraphrase). The shorthand appears on lines 10, 13, 183, 365, and 432 — user flagged as sounding AI-artificial.

Secondary fixes:

1. Line 194 — drop `The caveat:` colon construction; merge into the next sentence.
2. Line 365 — remove `This subsection is a quantitative deep dive for the motivated reader`; start with the content itself.
3. Line 192 — period instead of colon after `Prediction 1`.
4. Line 13, 365 — drop `directly contradicted` / `flatly contradicted` adverbs; let the verb carry the weight.

See ai_detection.md for full proposed revisions.
