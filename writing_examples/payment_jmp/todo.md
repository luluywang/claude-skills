The appenidices are a mess.

1. The goal of each of the reduced form appendix sections is to make a specific point that was helpful in setting up the model. Check where each section is cited and make sure the point in the intro paragraph is clear.

2. The additional counterfactuals section is messed up. The reader obviously does not need to know the names of the latex files. The main thing the reader wants to know is that it's possible to estimate different versions of the model with different assumptions and that the counterfactual results are quantitatively similar (for the scenarios that aren't debit robustness) and qualitatively similar for the debit robustness scenario.

3. In general, just go through the appendices and ask if each part clearly addresses a particular concern raised by a referee or something in the main text. Then re-write so that it does.