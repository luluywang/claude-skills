# No Pause in Beamer

Never use `\pause`, `\onslide`, `\only`, `\uncover`, or any overlay commands.

## Alternatives
- Create separate slides to build content progressively
- Apply color to highlight key points
- Use dedicated slides for pacing transitions

Reviewers recommending overlay implementation should be disregarded.
